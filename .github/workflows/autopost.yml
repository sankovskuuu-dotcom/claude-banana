name: Auto Post to Telegram

on:
  schedule:
    - cron: '0 7 * * *'
    - cron: '0 16 * * *'
  workflow_dispatch:

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Generate and post content
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          CHANNEL_ID: '@claude_banana_2025'
        run: |
          node -e "
          const https = require('https');
          const FREE_MODELS = ['meta-llama/llama-3.2-3b-instruct:free','nvidia/nemotron-nano-9b-v2:free','openai/gpt-oss-20b:free'];
          const TOPICS = [
            'Напиши пост про пользу промптинга для бизнеса с конкретными примерами.',
            'Напиши пост про то как AI экономит время. Добавь цифры.',
            'Напиши мотивационный пост для тех кто хочет изучить AI.',
            'Напиши пост про инструменты AI для создания видео контента.',
            'Напиши пост про автоматизацию рутины с помощью AI.',
            'Напиши пост-кейс: человек внедрил AI и что изменилось в его жизни.',
            'Напиши пост про Canva AI и как им пользоваться.',
            'Напиши пост про промпты для создания контента в Instagram.',
            'Напиши пост про то как AI помогает фрилансерам зарабатывать больше.',
          ];
          const topic = TOPICS[Math.floor(Math.random() * TOPICS.length)];
          async function callAI() {
            for (const model of FREE_MODELS) {
              try {
                const postData = JSON.stringify({model,messages:[{role:'system',content:'ВАЖНО: Пиши ТОЛЬКО на русском языке! Ты контент-менеджер Telegram-канала Claude + Banana. Создаёшь вовлекающие посты об AI. Стиль: живой, энергичный, с эмодзи. Длина: 150-200 слов. В конце: 👉 Присоединяйся: https://t.me/+8sIpW4azEBJjYjhi'},{role:'user',content:topic}],max_tokens:600});
                const result = await new Promise((resolve,reject) => {
                  const options = {hostname:'openrouter.ai',path:'/api/v1/chat/completions',method:'POST',headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(postData),'Authorization':'Bearer '+process.env.OPENROUTER_API_KEY,'HTTP-Referer':'https://sankovskuuu-dotcom.github.io/claude-banana/','X-Title':'Claude + Banana Bot'}};
                  const req = https.request(options,(res) => {let data='';res.on('data',chunk=>data+=chunk);res.on('end',()=>resolve(JSON.parse(data)));});
                  req.on('error',reject);req.write(postData);req.end();
                });
                if (result.choices?.[0]?.message?.content) return result.choices[0].message.content;
              } catch(e) { continue; }
            }
            return null;
          }
          async function sendToTelegram(text) {
            const postData = JSON.stringify({chat_id:process.env.CHANNEL_ID,text,parse_mode:'Markdown'});
            return new Promise((resolve,reject) => {
              const options = {hostname:'api.telegram.org',path:'/bot'+process.env.BOT_TOKEN+'/sendMessage',method:'POST',headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(postData)}};
              const req = https.request(options,(res) => {let data='';res.on('data',chunk=>data+=chunk);res.on('end',()=>resolve(JSON.parse(data)));});
              req.on('error',reject);req.write(postData);req.end();
            });
          }
          (async () => {
            const post = await callAI();
            if (post) { const r = await
